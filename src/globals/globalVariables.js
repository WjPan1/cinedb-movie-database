// Create a variable to store the title displayed on the browser tab
const APP_NAME = "CinDB";

// Create a variable to store the image paths relative to the public folder root
const imageFolderPath = process.env.PUBLIC_URL + '/assets/images/';


export { APP_NAME, imageFolderPath };
